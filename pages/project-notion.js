import Head from 'next/head';

const Notion = (page) => {
  return (
    <div>
      <Head>
        <title>CAT | Project | Notion</title>
        <link rel='icon' href='/favicon.ico' />
      </Head>
    </div>
  );
};
export default Notion;
